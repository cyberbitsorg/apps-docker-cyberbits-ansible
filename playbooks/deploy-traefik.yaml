---
- name: Deploy Traefik Reverse Proxy
  hosts: traefik
  become: yes
  gather_facts: yes

  roles:
    - role: common
      tags: ['common']

    - role: traefik
      tags: ['traefik']

  tasks:
    - name: Display traefik status
      debug:
        msg:
          - "Traefik deployed successfully!"
